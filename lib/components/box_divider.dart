import 'package:flutter/material.dart';

class BoxDivider extends StatelessWidget {
  const BoxDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 40,
    );
  }
}
